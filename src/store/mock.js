import predictBan from '@/mock/response/predictBan.json';
import predictPick from '@/mock/response/predictPick.json';
import predictRelation from '@/mock/response/predictRelation.json';

export const DUMMY_RESPONSE = {
  predictBan: predictBan,
  predictPick: predictPick,
  predictFriend: predictRelation,
  predictCounter: predictRelation,
};
